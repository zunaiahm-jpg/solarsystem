{
  "name": "solarsystem",
  "private": true,
  "version": "1.0.0",
  "description": "Static WebGL solar system explorer (Three.js via CDN import map)",
  "scripts": {
    "dev": "serve . -l 3000 --no-clipboard",
    "start": "serve . -l 3000 --no-clipboard"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
